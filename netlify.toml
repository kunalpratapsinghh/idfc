[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  # GITHUB_TOKEN must be set in Netlify dashboard → Site settings → Environment variables

[[plugins]]
  package = "@netlify/plugin-nextjs"
